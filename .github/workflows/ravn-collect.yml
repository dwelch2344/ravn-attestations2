# Generated by Ravn. Runs the approved collector in this repository.
#
# The @<sha> is load-bearing, not hygiene: GitHub bakes it into the signing
# certificate, and Ravn checks it against its published approved list. A tag
# would resolve to whatever it points at on the day you run.
name: ravn collect

on:
  workflow_dispatch: {}

jobs:
  collect:
    uses: RavnSecurity/ravn-actions/.github/workflows/collect-profile.yml@356d5504acf8457af81964318a1a289b1288d8f3
    secrets:
      RAVN_READONLY_TOKEN: ${{ secrets.RAVN_READONLY_TOKEN }}
      H1_API_IDENTIFIER: ${{ secrets.H1_API_IDENTIFIER }}
      H1_API_TOKEN: ${{ secrets.H1_API_TOKEN }}
    permissions:
      contents: read
      id-token: write
      attestations: write